package bowwars.listeners;

import bowwars.Main;
import org.bukkit.event.Listener;

public class AreaListener implements Listener {
    private final Main plugin;

    public AreaListener(Main plugin) {
        this.plugin = plugin;
    }
    
}
